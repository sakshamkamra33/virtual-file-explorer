server {
    listen 80;
    
    location / {
        root /usr/share/nginx/html;
        index index.html index.htm;
        
        # This is essential for React Router (Single Page Applications)
        # It redirects all 404s back to index.html so React can handle the routing
        try_files $uri $uri/ /index.html;
    }
}
